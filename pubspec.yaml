name: absensi_del
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.0
  flutter_image_compress: ^2.1.0
  path_provider: ^2.0.0
  path: ^1.8.0
  http: ^1.0.0




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
