name: absensi_del
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  camera: ^0.10.5+9 # Atau versi terbaru
  path_provider: ^2.1.3 # Atau versi terbaru
  path: ^1.8.3 # Atau versi terbaru
  intl: ^0.19.0 # <--- TAMBAHKAN BARIS INI




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
